const Logo = () => {
  return (
    <nav className="flex w-full py-8   justify-between">
      <div>logo</div>
      <div>logo2</div>
      <button>Login</button>
    </nav>
  );
};

export default Logo;
